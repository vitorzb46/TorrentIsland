<ui:FluentWindow
    x:Class="TorrentIsland.Presentation.Player.PlayerWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vlc="clr-namespace:LibVLCSharp.WPF;assembly=LibVLCSharp.WPF"
    AllowDrop="True"
    DragEnter="PlayerWindow_DragEnter"
    DragOver="PlayerWindow_DragOver"
    Drop="PlayerWindow_Drop"
    Width="1280"
    Height="720"
    Background="black"
    ExtendsContentIntoTitleBar="True"
    MouseMove="Player_Mouse"
    WindowBackdropType="None"
    WindowStartupLocation="CenterScreen">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />

            <RowDefinition Height="*" />

        </Grid.RowDefinitions>

        <Grid.Resources>
            <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        </Grid.Resources>


        <ui:TitleBar
            x:Name="MyTitleBar"
            Title="Torrent Island"
            Grid.Row="0"
            Height="32"
            Background="#FF1A1A1A"
            Foreground="White"
            Icon="pack://application:,,,/Resources/app_icon.ico" />


        <Grid Grid.Row="1" Background="Black">


            <vlc:VideoView
                x:Name="VideoView"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                MouseMove="Player_Mouse" />


            <Grid
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">

                <Border
                    Padding="32,28"
                    Background="#CC0A0A0A"
                    CornerRadius="16">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">


                        <ui:ProgressRing
                            Width="45"
                            Height="45"
                            IsIndeterminate="True"
                            Progress="0" />

                        <TextBlock
                            Margin="0,16,0,0"
                            HorizontalAlignment="Center"
                            FontFamily="Segoe UI Variable Display, Bahnschrift"
                            FontSize="15"
                            FontWeight="SemiBold"
                            Foreground="White"
                            Text="Carregando..." />
                    </StackPanel>
                </Border>
            </Grid>

        </Grid>

    </Grid>
</ui:FluentWindow>