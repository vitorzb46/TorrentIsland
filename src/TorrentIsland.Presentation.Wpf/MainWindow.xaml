<Window x:Class="TorrentIsland.Presentation.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TorrentIsland.Presentation.Wpf"
        mc:Ignorable="d"
        Title="Torrent Island" Height="450" Width="800">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="MagnetTextBox" VerticalContentAlignment="Center"
                     ToolTip="Cole o magnet link aqui" />
            <Button x:Name="StreamarButton" Grid.Column="1" Margin="8,0,0,0"
                    Content="Abrir no Player" Click="Streamar_Click" />
        </Grid>

        <ListBox x:Name="LogsListBox" Grid.Row="1" />
    </Grid>
</Window>
